replicaCount: 2
image:
  repository: ghcr.io/berhot/platform-analytics-core
  pullPolicy: IfNotPresent
  tag: "latest"
service:
  type: ClusterIP
  port: 8000
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilization: 70
health:
  liveness:
    path: /health
    port: 8000
    initialDelaySeconds: 15
    periodSeconds: 20
  readiness:
    path: /health
    port: 8000
    initialDelaySeconds: 5
    periodSeconds: 10
env:
  APP_ENV: production
